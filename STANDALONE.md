# Standalone export

This tree was generated by `scripts/export-standalone-npm-package.sh tree-spec-editor` from the DeliveryPlus monorepo.

```bash
git init
git branch -M main
git add .
git commit -m "Sync tree-spec-editor from DeliveryPlus"
git remote add origin git@github.com:SignalSafeSoftware/tree-spec-editor.git
git push -u origin main
```

See `RELEASING.md` for npm publish steps.
